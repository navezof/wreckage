using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConditionManagerUI : MonoBehaviour
{
    // [SerializeField] private GameObject conditionUIPrefab;
    // private ConditionManager conditionManager;


    // // Start is called before the first frame update
    // void Start()
    // {
    //     CharacterUI characterUI = GetComponentInParent<CharacterUI>();

    //     conditionManager = characterUI.Character.GetComponent<ConditionManager>();
    //     foreach (Condition stat in conditionManager.ConditionList)
    //     {
    //         GameObject newConditionUI = Instantiate(conditionUIPrefab, this.transform);
    //         ConditionUI conditionUI = newConditionUI.GetComponent<ConditionUI>();
    //         conditionUI.LinkToStat(stat);
    //     }
    // }
}
